spring:
  profiles:
    active: dev

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/girl?useSSL=true
    username: root
    password: 801023

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

